{
  "name": "babel-plugin-add-filehash",
  "version": "6.9.1",
  "description": "Babel plugin that makes sure the Babel output changes whenever the input file changes.",

  "repository": "sth/babel-plugin-add-filehash",
  "license": "MIT",
  "main": "lib/index.js",
  "keywords": [
    "babel-plugin"
  ],
  "scripts": {
    "build": "babel src --out-dir lib",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "babel-runtime": "^6.0.0",
    "babel-types": "^6.8.0"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0"
  }
}
